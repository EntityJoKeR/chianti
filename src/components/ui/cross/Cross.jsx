import "./Cross.module.css"

const Cross = (props) => {
    return (
        <div className="Cross">
            <img src="/CROSS.svg" className="CrossImg"/>
        </div>
    );
};

export default Cross;